<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme — used in non-launch contexts -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Theme for most of the app — no action bar, allows full-screen WebView -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Status bar color matches Memora brand dark purple -->
        <item name="android:statusBarColor">#1a0a2e</item>
        <item name="android:navigationBarColor">#1a0a2e</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen theme — shown immediately before WebView loads -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <item name="android:statusBarColor">#1a0a2e</item>
        <item name="android:navigationBarColor">#1a0a2e</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

</resources>